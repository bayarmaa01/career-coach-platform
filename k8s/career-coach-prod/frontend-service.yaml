apiVersion: v1
kind: Service
metadata:
  name: frontend-service
  namespace: career-coach-prod
  labels:
    app: frontend-prod
    tier: frontend
spec:
  type: NodePort
  ports:
  - port: 3100
    targetPort: 80
    protocol: TCP
    nodePort: 31000
  selector:
    app: frontend-prod
