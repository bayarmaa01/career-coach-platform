apiVersion: v1
kind: Service
metadata:
  name: frontend-service
  namespace: career-coach-prod
  labels:
    app: frontend-prod
    tier: frontend
spec:
  type: ClusterIP
  ports:
  - port: 3100
    targetPort: 80
    protocol: TCP
  selector:
    app: frontend-prod
