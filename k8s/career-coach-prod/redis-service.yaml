apiVersion: v1
kind: Service
metadata:
  name: redis-service
  namespace: career-coach-prod
  labels:
    app: redis-prod
    tier: cache
spec:
  type: ClusterIP
  ports:
  - port: 6379
    targetPort: 6379
    protocol: TCP
  selector:
    app: redis-prod
