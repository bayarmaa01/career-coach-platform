apiVersion: v1
kind: Service
metadata:
  name: backend-service
  namespace: career-coach-prod
  labels:
    app: backend-prod
    tier: backend
spec:
  type: ClusterIP
  ports:
  - port: 4100
    targetPort: 5000
    protocol: TCP
  selector:
    app: backend-prod
