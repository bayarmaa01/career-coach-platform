apiVersion: v1
kind: Service
metadata:
  name: prometheus-service
  namespace: career-coach-prod
  labels:
    app: prometheus
    tier: monitoring
spec:
  type: ClusterIP
  ports:
  - port: 9090
    targetPort: 9090
    protocol: TCP
  selector:
    app: prometheus
