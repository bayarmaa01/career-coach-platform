apiVersion: v1
kind: Service
metadata:
  name: grafana-service
  namespace: career-coach-prod
  labels:
    app: grafana
    tier: monitoring
spec:
  type: ClusterIP
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
  selector:
    app: grafana
